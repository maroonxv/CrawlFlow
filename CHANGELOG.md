# 更新日志

仓库中所有值得记录的重要变更都应写入本文件。

## [未发布]

### 新增

- 新增仓库治理文档：许可证、贡献指南、行为准则与安全策略。
- 新增 GitHub Issue 模板、拉取请求模板与 Dependabot 配置。
- 新增文档索引与仓库级测试说明。
- 新增 `backend/.env.example`，便于更安全地完成本地初始化。

### 变更

- 将顶层文档目录从 `doc/` 统一标准化为 `docs/`。
- 将仓库级测试目录从 `test/` 统一标准化为 `tests/`。
- 将命令说明与项目 TODO 记录迁移到 `docs/` 下集中管理。
- 将后端 PlantUML 资源迁移到 `backend/docs/` 下。
- 重写根目录 `README.md`，使其成为更清晰的仓库入口文档。
